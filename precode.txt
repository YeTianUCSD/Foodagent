Step 1: Find out how long people sleep in average
Step 2: use the result from step 1 to search on google and get the top website that answers the question how long people sleep in average.
Step 3: Extract the text from the top website and return it as the final answer.



```python
task_result = self.execute_task('google_search', ['how long people sleep in average'])
top_website_url = task_result['url']
text = self.execute_task('extract_text', [top_website_url])
```

```python
# define the tools
def google_search(search_query):
    # this function should call self.execute_task with the appropriate arguments
    return self.execute_task('google_search', [search_query])

def extract_text(url):
    # this function should call self.execute_task with the appropriate arguments
    return self.execute_task('extract_text', [url])

# define the steps
def step1():
    search_query = 'how long people sleep in average'
    search_result = google_search(search_query)
    return search_result

def step2():
    top_website_url = step1()['url']
    return top_website_url

def step3():
    top_website_url = step2()
    text = self.execute_task(